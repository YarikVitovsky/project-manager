# API Configuration
# For local development, use /api (served by Vercel CLI)
# For production, Vercel handles this automatically
REACT_APP_API_URL=/api

# Build Configuration
CI=false
GENERATE_SOURCEMAP=false
