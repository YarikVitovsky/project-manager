const express = require('express');
const router = express.Router();

// In-memory array for tasks (replace with DB later)
const tasks = [];

// TODO: Add your API logic here

module.exports = router;
